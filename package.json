{
  "name": "figma-unagency",
  "version": "1.0.0",
  "description": "The unAgency website",
  "private": true,
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "framer-motion": "^11.0.0",
    "lucide-react": "^0.300.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet-async": "^2.0.5",
    "react-scripts": "5.0.1",
    "resend": "^3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build:19hpetfood": "npm install --prefix 19hpetfood && npm run build --prefix 19hpetfood && rm -rf public/19hpetfood.com && mkdir -p public/19hpetfood.com && cp -R 19hpetfood/out/. public/19hpetfood.com/",
    "build": "npm run build:19hpetfood && (react-scripts build || CI=false react-scripts build)",
    "build:seo": "npm run build && node scripts/generate-static-html.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
